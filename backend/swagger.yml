openapi: 3.0.0
info:
  title: AWS-Test
  description: API Docs for AWS-Test server
  version: 0.0.1
servers:
  - url: https://3.17.164.221:5000
paths:
  /test:
    get:
      summary: Returns a JSON containing a message.
      responses:
        200:
          description: "Success"
          schema:
            type: object
            properties:
              message:
                type: string